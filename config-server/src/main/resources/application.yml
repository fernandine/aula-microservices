server:
  port: 8888
  
spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/fernandine/aula-microservices

          default-label: main
          search-paths:
          - 'greeting-service*'